# This file serves as an example for the required environment variables.
# For local development, copy this file to .env.local and fill in the values.
# For production, create a .env.production file with the correct values.

# The full connection string for the PostgreSQL database.
# The user, password, host, port, and database name should be configured correctly.
DATABASE_URL="postgresql://piano_learner_user:YOUR_DATABASE_PASSWORD_HERE@localhost:5432/piano_learner_db?schema=public"

# A secret for NextAuth.js.
# You can generate a new one with the command: openssl rand -hex 32
NEXTAUTH_SECRET="YOUR_NEXTAUTH_SECRET_HERE"

# The public URL of your application.
# This is used by NextAuth.js for callbacks and other functions.
NEXTAUTH_URL="http://localhost:3000"